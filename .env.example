# Environment configuration for Maintenance Engineer Copilot
# Optional: Provide API key if connecting external frontier LLM (otherwise offline grounded RAG is used)
OPENAI_API_KEY=
GROQ_API_KEY=
GEMINI_API_KEY=

# Server Configuration
FASTAPI_PORT=8000
STREAMLIT_PORT=8501
DATABASE_PATH=database/maintenance.db
